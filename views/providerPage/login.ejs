<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> - TrackSmart</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link href="/css/style.css" rel="stylesheet" />
    <style>
      .redirect-container {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      }
      .redirect-card {
        background: white;
        border-radius: 15px;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
        padding: 3rem;
        width: 100%;
        max-width: 500px;
        text-align: center;
      }
      .logo {
        text-align: center;
        margin-bottom: 2rem;
      }
      .logo h2 {
        color: #667eea;
        font-weight: bold;
      }
      .btn-redirect {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        border-radius: 8px;
        padding: 12px 30px;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: white;
        text-decoration: none;
        display: inline-block;
        margin: 10px;
        transition: all 0.3s ease;
      }
      .btn-redirect:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        color: white;
        text-decoration: none;
      }
      .countdown {
        font-size: 1.2em;
        color: #667eea;
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <div class="redirect-container">
      <div class="redirect-card">
        <div class="logo">
          <h2>TrackSmart</h2>
          <p class="text-muted">Logistics Provider Portal</p>
        </div>

        <div class="mb-4">
          <i class="fas fa-truck fa-4x text-success mb-3"></i>
          <h4>Login System Updated!</h4>
          <p class="text-muted">
            We've upgraded our login system! All provider logins are now handled
            through our unified login portal.
          </p>
        </div>

        <div class="mb-4">
          <p class="mb-2">
            You will be redirected automatically in
            <span class="countdown" id="countdown">5</span> seconds...
          </p>
          <p class="text-muted">Or click the button below to go there now:</p>
        </div>

        <div>
          <a href="/main-login" class="btn-redirect">
            <i class="fas fa-sign-in-alt me-2"></i>Go to Login Portal
          </a>
        </div>

        <div class="mt-4">
          <small class="text-muted">
            <strong>Note:</strong> Use your existing provider credentials on the
            new login page by selecting "Logistics Provider" login option.
          </small>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      // Countdown and auto-redirect
      let countdown = 5;
      const countdownElement = document.getElementById("countdown");

      const timer = setInterval(() => {
        countdown--;
        countdownElement.textContent = countdown;

        if (countdown <= 0) {
          clearInterval(timer);
          window.location.href = "/main-login";
        }
      }, 1000);

      // Optional: Allow users to cancel auto-redirect by interacting with the page
      document.addEventListener("click", () => {
        clearInterval(timer);
        countdownElement.textContent = "cancelled";
      });
    </script>
  </body>
</html>
